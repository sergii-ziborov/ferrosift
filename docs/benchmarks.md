# Benchmarks

Generated by `cargo xtask bench report` from Criterion's own estimates.
Every figure is a median; nothing here is selected, and a result that
goes against FerroSift is printed the same way as one that does not.

## Why you should believe this

A benchmark published by the project it flatters is worth nothing
unless it is arranged so that rigging it would be visible. Six things
make that true here.

**Every size is printed, with a verdict on each row.** There is no
summary that quotes the favourable end of a sweep. If FerroSift loses
at 16 bytes and wins at 1 MiB, both rows say so in the same column.

**The baseline is the fastest available competitor, not a convenient
one.** Where two crates do the same job — `hex` and `faster-hex` —
both are measured and the quicker one is what the verdict compares
against. This rule was added after an earlier run reported a win over
`hex` while `faster-hex` existed and had not been tried.

**Competitors are called through their documented fast path**, at
exactly pinned versions, with no settings changed to slow them down.

**FerroSift is handicapped, not helped.** It runs through its full
public surface — registry lookup, argument resolution, budget checks —
and additionally copies its input on every iteration. Reaching past
that to the raw codec would compare two different amounts of work.

**The inputs are deterministic and the generator is in this
repository**, so anyone can produce the same bytes and check.

**The raw estimates are committed** next to this file as
`benchmarks.json`, and the machine and compiler are recorded below. A
reader who does not trust the prose can recompute every ratio from the
data, or re-run the whole thing with one command.

**A ratio is only stated when the run can support it.** Where the two
confidence intervals overlap the verdict reads *no measurable
difference* rather than picking a direction, and a measurement whose
own interval is wider than 15% of its median is marked *noisy* and
reported as no result at all. This cuts both ways and is meant to: an
earlier run on a loaded machine produced a FerroSift digest that
appeared faster than the primitive it calls, which is impossible, and
nothing in the report at the time would have stopped that being read
as a win.

## Measured on

| | |
|---|---|
| Compiler | rustc 1.97.1 (8bab26f4f 2026-07-14) |
| Platform | windows / x86_64 |
| CPU | Intel64 Family 6 Model 181 Stepping 0, GenuineIntel |

| Batch | Commit | Working tree |
|---|---|---|
| digest | `d021fb17` | clean |
| dispatch | `d021fb17` | clean |
| encoding | `b2c9bed7` | **uncommitted changes present** |

## Reproducing

Batches are measured independently, so only what changed needs
re-measuring. At this catalog size a full sweep is minutes; at five
hundred operations it is an afternoon, and an afternoon spent
re-measuring untouched code is an afternoon nobody spends — which is
how published numbers go stale.

```bash
cargo xtask bench check   # which batches predate a change they cover
cargo xtask bench stale   # re-run only those, then rebuild this file
```

`cargo xtask bench run encoding` measures one batch by name, and
`cargo xtask bench all` still does everything.

The comparison against the reference is a separate step, because it
needs the pinned CyberChef checkout that the corpus already uses:

```bash
cargo xtask bench reference   # measure CyberChef, then rebuild this file
```

The toolchain is pinned in `bench/rust-toolchain.toml`, every
comparison crate is pinned to an exact version, `bench/Cargo.lock` is
committed, and the optimisation settings are written out in
`bench/Cargo.toml` rather than inherited. Timings will differ between
machines; ratios should not.

## What is being compared

Three different questions get three different kinds of arm.

**Against a specialist crate.** `base64`, `hex`, `crc32fast`, and
`strsim` each do one thing and are among the fastest Rust
implementations of it. Where FerroSift wrote the algorithm itself,
this is a real comparison.

**Against the primitive.** For the digests and ciphers, FerroSift *is*
RustCrypto — the same code computing the same bytes. Reporting a win
there would be measuring nothing. The useful question is the opposite:
what does going through a recipe cost above calling the primitive
directly? That gap is the library's own overhead and the only part
FerroSift is answerable for.

**Between entry points.** `execute-each-call` resolves the recipe
against the registry every time; `compiled-pipeline` resolves once.
The difference is what compiling buys.

FerroSift is always driven through its public surface, arguments and
budget checks included. Reaching past that to the codec would compare
two different amounts of work.

## Method

Inputs are a seeded xorshift, so a rerun measures the code rather than
the data. Sizes sweep from 16 B to 1 MiB because per-call overhead
decides the small end and the algorithm decides the large end — a
comparison quoting only one of them is choosing its own answer.

## Where this stands

Two findings, and they point opposite ways. Both are stated here
rather than left for a reader to assemble from the tables.

**FerroSift is not currently faster than any best-in-class specialist
crate, at any size measured.** Every competitive comparison below is a
loss.

**FerroSift is faster than the reference it ports, by more than an
order of magnitude everywhere it was measured.** That is the weaker
claim of the two and the one worth less: beating a JavaScript
implementation with a Rust one is the least a port should manage, and
it says nothing about whether the Rust is good. The specialist crates
answer that question, and they answer it unfavourably.

An earlier version of this file claimed a win over the `hex` crate at
64 KiB and above. The claim was arithmetically true and worthless:
`faster-hex` implements the same function with SIMD, had not been
measured, and beats both. Against it FerroSift is between 4.8× and
33.6× slower. The win is retracted, and the rule that produced it —
compare against whichever crate happens to be in the file — has been
replaced by one that compares against the fastest arm in the group.

Two results are genuine and neither is a competitive claim. Compiling
a pipeline is measurably faster than resolving a recipe on every call,
which is FerroSift against itself and the first evidence that the
compiled path earns its place. And at large inputs the cost of going
through a recipe rather than calling a digest directly is small, which
says the library layer is thin — not that the library is fast.

The gaps have a shape. Roughly half a microsecond of fixed per-call
cost decides everything below about four kilobytes, which is why the
16-byte rows are so lopsided against crates that are one function.
Above that the algorithms are the measurement, and the ports are
written for exactness against the reference rather than for speed:
they iterate `char` where bytes would do, allocate per chunk, and
carry validation the specialist crates do not.

None of that is an excuse and none of it is stated as one. They are
the reasons, and each is a thing to fix.

This harness exists to make that work visible, and it already has. The
first thing it found was a base64 decoder scanning a 64-symbol list
for every character, several times per character; replacing it with a
lookup table made decoding 13 times faster at 1 MiB and cut the gap
from 118× to 7×. The corpus confirmed the output did not change. That
is the loop this file is here to run.

One caution about the `overhead` rows, learned by re-running them. An
earlier report put the cost of going through a recipe at 1.07× to
1.71× above calling the digest directly, and read that as evidence
the library layer is thin. A second run on the same code could not
reproduce it: most of those rows came back noisy or as no measurable
difference. Gaps that small are at this machine's noise floor, and a
single run cannot settle them. The large gaps in every other group
reproduced without trouble.

## Against the reference itself

Every figure here is a **floor**, not a headline. A row states what
survives reading both sides as unfavourably as the data allows — the
reference at its fastest batch against FerroSift at the slow end of
its interval. The ratio of the medians is larger than the number
printed, and is not printed. Where the two ranges touch at all there
is no verdict, however tight the batches happened to be.

Node is far noisier than criterion, so a spread is shown wherever the
reference's own batches disagreed by more than 15%. A floor drawn from
noisy batches and one drawn from tight batches are not the same claim
even when they read alike.

This is the comparison that answers *is the port worth having*. It is
not evidence that the code is fast — the specialist crates below say
it is not — only that a Rust library beats a JavaScript one at the
same work, which is the least one should expect of a port.

### base64/encode

| Size | CyberChef | FerroSift | Verdict |
|---:|---:|---:|---|
| 16 B | 42.80 µs | 1.24 µs | at least 32.4× faster *(±60%)* |
| 256 B | 50.25 µs | 1.73 µs | at least 26.2× faster |
| 4 KiB | 139.84 µs | 4.14 µs | at least 32.2× faster |
| 64 KiB | 3.11 ms | 62.67 µs | at least 39.5× faster *(±28%)* |
| 1 MiB | 102.39 ms | 1.30 ms | at least 64.4× faster *(±18%)* |

### hex/encode

| Size | CyberChef | FerroSift | Verdict |
|---:|---:|---:|---|
| 16 B | 121.95 µs | 733 ns | at least 99.7× faster *(±61%)* |
| 256 B | 113.93 µs | 824 ns | at least 98.1× faster *(±87%)* |
| 4 KiB | 498.29 µs | 4.44 µs | at least 97.7× faster *(±17%)* |
| 64 KiB | 8.64 ms | 67.45 µs | at least 102.0× faster *(±52%)* |
| 1 MiB | 255.74 ms | 1.57 ms | at least 155.1× faster |

### overhead/md5

| Size | CyberChef | FerroSift | Verdict |
|---:|---:|---:|---|
| 16 B | 72.46 µs | 1.25 µs | at least 29.8× faster *(±119%)* |
| 256 B | 63.34 µs | 1.66 µs | at least 31.3× faster *(±49%)* |
| 4 KiB | 388.97 µs | 8.81 µs | at least 29.5× faster *(±52%)* |
| 64 KiB | 6.49 ms | 124.95 µs | at least 37.9× faster *(±52%)* |
| 1 MiB | 118.66 ms | 2.08 ms | at least 48.5× faster *(±21%)* |

### overhead/sha256

| Size | CyberChef | FerroSift | Verdict |
|---:|---:|---:|---|
| 16 B | 55.75 µs | 1.50 µs | at least 32.0× faster *(±17%)* |
| 256 B | 70.22 µs | 1.76 µs | at least 35.8× faster *(±31%)* |
| 4 KiB | 404.84 µs | 3.69 µs | at least 90.4× faster *(±26%)* |
| 64 KiB | 6.61 ms | 33.64 µs | at least 161.1× faster *(±28%)* |
| 1 MiB | 111.50 ms | 745.79 µs | at least 140.2× faster *(±22%)* |

### cipher/tea

| Size | CyberChef | FerroSift | Verdict |
|---:|---:|---:|---|
| 16 B | 124.49 µs | 2.58 µs | at least 36.4× faster *(±38%)* |
| 256 B | 187.33 µs | 4.81 µs | at least 21.3× faster *(±76%)* |
| 4 KiB | 975.94 µs | 44.94 µs | at least 16.8× faster *(±46%)* |
| 64 KiB | 17.26 ms | 1.02 ms | at least 12.3× faster *(±40%)* |
| 1 MiB | 402.11 ms | 12.21 ms | at least 29.7× faster |

### cipher/xtea

| Size | CyberChef | FerroSift | Verdict |
|---:|---:|---:|---|
| 16 B | 152.90 µs | 1.83 µs | at least 70.1× faster *(±88%)* |
| 256 B | 206.86 µs | 7.25 µs | at least 20.7× faster *(±61%)* |
| 4 KiB | 944.12 µs | 46.36 µs | at least 15.1× faster *(±40%)* |
| 64 KiB | 12.23 ms | 786.34 µs | at least 12.6× faster |
| 1 MiB | 336.89 ms | 13.00 ms | at least 23.1× faster |

## Against the other Rust port

Measured on **linux**, against rx-chef at `99e26de96e28`,
unmodified.

*unmodified rx-chef does not link on Windows MSVC, so this arm is measured on Linux and is not comparable with the rest of this report.*

This is the comparison that asks whether a library of *this* shape —
registry, operation trait, typed arguments, pipeline — carries its
structure cheaply. Both sides pay that cost, which is what makes the
answer about the implementations rather than about the architecture.
The specialist crates below cannot answer it: beating `base64` would
mean our codec is good, and losing to it says as much about the
dispatch layer as about the codec.

A ratio here is the ratio of the medians, not a floor. The gaps are
small enough that the two intervals overlap on many rows, and refusing
a verdict for all of them would hide a result several runs agree on —
so an overlap is marked rather than silently dropped.

Both arms are timed one after the other in one process, which is the
best available and is not exact: a machine that slows between them
biases the ratio. Two runs of one binary have disagreed here by a
quarter. Read the direction, not the digit.

### base64-encode

| Size | `ferrosift` | `rx-chef` | Ratio |
|---:|---:|---:|---|
| 16 B | 399 ns | 440 ns | 1.10× faster |
| 256 B | 515 ns | 523 ns | 1.02× faster *(intervals overlap)* |
| 4 KiB | 2.31 µs | 1.75 µs | 1.32× slower |
| 64 KiB | 35.32 µs | 21.80 µs | 1.62× slower |
| 1 MiB | 414.71 µs | 362.94 µs | 1.14× slower |

### hex-encode

| Size | `ferrosift` | `rx-chef` | Ratio |
|---:|---:|---:|---|
| 16 B | 142 ns | 493 ns | 3.47× faster |
| 256 B | 249 ns | 5.98 µs | 24.03× faster |
| 4 KiB | 2.17 µs | 93.03 µs | 42.81× faster |
| 64 KiB | 33.26 µs | 1.52 ms | 45.57× faster |
| 1 MiB | 568.73 µs | 25.56 ms | 44.95× faster |

### rot13

| Size | `ferrosift` | `rx-chef` | Ratio |
|---:|---:|---:|---|
| 16 B | 197 ns | 100 ns | 1.97× slower |
| 256 B | 376 ns | 615 ns | 1.64× faster |
| 4 KiB | 4.79 µs | 10.20 µs | 2.13× faster |
| 64 KiB | 387.79 µs | 407.20 µs | 1.05× faster |
| 1 MiB | 4.74 ms | 6.58 ms | 1.39× faster |

## Claims

This table is computed from the measurements, not written. A row can
only say *faster* if the subject beat the fastest comparison arm at
that size, on a run that was not noisy, with confidence intervals that
do not overlap — and the win has to hold across more than one size.
There is no way to add a claim here by editing text.

That last rule was added because the first three were not enough. A
digest measured *faster through a recipe than by calling the primitive
the recipe calls* passed all of them: both intervals were narrow, they
did not overlap, and neither run looked noisy. It was still impossible.
The baseline had simply had a bad run at one size, and nothing about a
confidence interval — which describes how repeatable one measurement
is — could see that the measurements either side of it disagreed.

Supported claims right now: **1** of 11 groups.

| Group | Compared against | Claim |
|---|---|---|
| base64 / decode | `base64-crate` | **no claim** — loses or ties at every size |
| base64 / encode | `base64-crate` | **no claim** — loses or ties at every size |
| checksum / adler32 | — | no comparison arm in this group |
| checksum / crc32 | — | no comparison arm in this group |
| cipher / tea | — | no comparison arm in this group |
| cipher / xtea | — | no comparison arm in this group |
| distance / levenshtein | `strsim-crate` | **no claim** — loses or ties at every size |
| hex / encode | `faster-hex-crate` | **no claim** — loses or ties at every size |
| overhead / identity | `execute-each-call` | 1.67× faster at 16 B, 1.74× faster at 4 KiB, 1.26× faster at 64 KiB |
| overhead / md5 | `primitive-direct` | **no claim** — loses or ties at every size |
| overhead / sha256 | `primitive-direct` | **no claim** — loses or ties at every size |

## Results

### base64 / decode

| Size | `base64-crate` | `ferrosift` | `ferrosift-per-call` | verdict |
|---:|---:|---:|---:|---|
| 16 B | 78 ns | 1.37 µs | 1.98 µs | noisy — rerun |
| 256 B | 215 ns | 1.85 µs | 2.68 µs | 8.62× slower |
| 4 KiB | 2.66 µs | 11.97 µs | 12.96 µs | 4.49× slower |
| 64 KiB | 42.54 µs | 213.68 µs | 199.49 µs | 5.02× slower |
| 1 MiB | 820.95 µs | 3.66 ms | 3.56 ms | 4.46× slower |

Verdict compares `ferrosift` against `base64-crate`, the fastest
comparison arm in this group.

<details><summary>Confidence intervals</summary>

| Size | Arm | Median | 95% interval |
|---:|---|---:|---|
| 16 B | `base64-crate` | 78 ns | 73 ns – 84 ns |
| 16 B | `ferrosift` | 1.37 µs | 1.27 µs – 1.49 µs |
| 16 B | `ferrosift-per-call` | 1.98 µs | 1.80 µs – 2.16 µs |
| 256 B | `base64-crate` | 215 ns | 211 ns – 219 ns |
| 256 B | `ferrosift` | 1.85 µs | 1.82 µs – 1.90 µs |
| 256 B | `ferrosift-per-call` | 2.68 µs | 2.59 µs – 2.80 µs |
| 4 KiB | `base64-crate` | 2.66 µs | 2.55 µs – 2.92 µs |
| 4 KiB | `ferrosift` | 11.97 µs | 11.85 µs – 12.49 µs |
| 4 KiB | `ferrosift-per-call` | 12.96 µs | 12.81 µs – 13.05 µs |
| 64 KiB | `base64-crate` | 42.54 µs | 40.43 µs – 45.64 µs |
| 64 KiB | `ferrosift` | 213.68 µs | 206.30 µs – 234.98 µs |
| 64 KiB | `ferrosift-per-call` | 199.49 µs | 191.29 µs – 213.15 µs |
| 1 MiB | `base64-crate` | 820.95 µs | 797.19 µs – 838.40 µs |
| 1 MiB | `ferrosift` | 3.66 ms | 3.42 ms – 3.85 ms |
| 1 MiB | `ferrosift-per-call` | 3.56 ms | 3.35 ms – 3.69 ms |

</details>

### base64 / encode

| Size | `base64-crate` | `ferrosift` | `ferrosift-per-call` | verdict |
|---:|---:|---:|---:|---|
| 16 B | 76 ns | 1.24 µs | 1.88 µs | 16.29× slower |
| 256 B | 217 ns | 1.73 µs | 2.45 µs | noisy — rerun |
| 4 KiB | 2.23 µs | 4.14 µs | 5.55 µs | 1.86× slower |
| 64 KiB | 37.21 µs | 62.67 µs | 68.08 µs | 1.68× slower |
| 1 MiB | 813.29 µs | 1.30 ms | 1.29 ms | 1.60× slower |

Verdict compares `ferrosift` against `base64-crate`, the fastest
comparison arm in this group.

<details><summary>Confidence intervals</summary>

| Size | Arm | Median | 95% interval |
|---:|---|---:|---|
| 16 B | `base64-crate` | 76 ns | 74 ns – 80 ns |
| 16 B | `ferrosift` | 1.24 µs | 1.16 µs – 1.29 µs |
| 16 B | `ferrosift-per-call` | 1.88 µs | 1.78 µs – 1.98 µs |
| 256 B | `base64-crate` | 217 ns | 197 ns – 232 ns |
| 256 B | `ferrosift` | 1.73 µs | 1.68 µs – 1.77 µs |
| 256 B | `ferrosift-per-call` | 2.45 µs | 2.32 µs – 2.52 µs |
| 4 KiB | `base64-crate` | 2.23 µs | 2.15 µs – 2.31 µs |
| 4 KiB | `ferrosift` | 4.14 µs | 4.00 µs – 4.29 µs |
| 4 KiB | `ferrosift-per-call` | 5.55 µs | 5.03 µs – 5.90 µs |
| 64 KiB | `base64-crate` | 37.21 µs | 35.79 µs – 39.69 µs |
| 64 KiB | `ferrosift` | 62.67 µs | 61.20 µs – 64.52 µs |
| 64 KiB | `ferrosift-per-call` | 68.08 µs | 64.82 µs – 72.22 µs |
| 1 MiB | `base64-crate` | 813.29 µs | 780.83 µs – 845.94 µs |
| 1 MiB | `ferrosift` | 1.30 ms | 1.25 ms – 1.36 ms |
| 1 MiB | `ferrosift-per-call` | 1.29 ms | 1.22 ms – 1.40 ms |

</details>

### checksum / adler32

| Size | `ferrosift` | verdict |
|---:|---:|---|
| 16 B | 984 ns | — |
| 256 B | 1.38 µs | — |
| 4 KiB | 4.69 µs | — |
| 64 KiB | 53.36 µs | — |
| 1 MiB | 760.09 µs | — |

<details><summary>Confidence intervals</summary>

| Size | Arm | Median | 95% interval |
|---:|---|---:|---|
| 16 B | `ferrosift` | 984 ns | 952 ns – 1.02 µs |
| 256 B | `ferrosift` | 1.38 µs | 1.26 µs – 1.45 µs |
| 4 KiB | `ferrosift` | 4.69 µs | 4.44 µs – 5.14 µs |
| 64 KiB | `ferrosift` | 53.36 µs | 52.59 µs – 55.25 µs |
| 1 MiB | `ferrosift` | 760.09 µs | 751.25 µs – 767.86 µs |

</details>

### checksum / crc32

| Size | `crc32fast-crate` | verdict |
|---:|---:|---|
| 16 B | 8 ns | — |
| 256 B | 15 ns | — |
| 4 KiB | 140 ns | — |
| 64 KiB | 2.12 µs | — |
| 1 MiB | 34.12 µs | — |

<details><summary>Confidence intervals</summary>

| Size | Arm | Median | 95% interval |
|---:|---|---:|---|
| 16 B | `crc32fast-crate` | 8 ns | 8 ns – 9 ns |
| 256 B | `crc32fast-crate` | 15 ns | 15 ns – 16 ns |
| 4 KiB | `crc32fast-crate` | 140 ns | 134 ns – 151 ns |
| 64 KiB | `crc32fast-crate` | 2.12 µs | 2.08 µs – 2.19 µs |
| 1 MiB | `crc32fast-crate` | 34.12 µs | 33.34 µs – 35.59 µs |

</details>

### cipher / tea

| Size | `ferrosift` | `ferrosift-per-call` | verdict |
|---:|---:|---:|---|
| 16 B | 2.58 µs | 2.91 µs | — |
| 256 B | 4.81 µs | 5.98 µs | — |
| 4 KiB | 44.94 µs | 47.11 µs | — |
| 64 KiB | 1.02 ms | 737.79 µs | — |
| 1 MiB | 12.21 ms | 18.16 ms | — |

<details><summary>Confidence intervals</summary>

| Size | Arm | Median | 95% interval |
|---:|---|---:|---|
| 16 B | `ferrosift` | 2.58 µs | 2.15 µs – 2.77 µs |
| 16 B | `ferrosift-per-call` | 2.91 µs | 2.85 µs – 2.95 µs |
| 256 B | `ferrosift` | 4.81 µs | 4.77 µs – 4.83 µs |
| 256 B | `ferrosift-per-call` | 5.98 µs | 5.94 µs – 6.02 µs |
| 4 KiB | `ferrosift` | 44.94 µs | 44.83 µs – 45.41 µs |
| 4 KiB | `ferrosift-per-call` | 47.11 µs | 46.79 µs – 47.30 µs |
| 64 KiB | `ferrosift` | 1.02 ms | 973.84 µs – 1.07 ms |
| 64 KiB | `ferrosift-per-call` | 737.79 µs | 734.90 µs – 740.57 µs |
| 1 MiB | `ferrosift` | 12.21 ms | 12.10 ms – 12.29 ms |
| 1 MiB | `ferrosift-per-call` | 18.16 ms | 17.22 ms – 18.66 ms |

</details>

### cipher / xtea

| Size | `ferrosift` | `ferrosift-per-call` | verdict |
|---:|---:|---:|---|
| 16 B | 1.83 µs | 2.91 µs | — |
| 256 B | 7.25 µs | 7.93 µs | — |
| 4 KiB | 46.36 µs | 57.19 µs | — |
| 64 KiB | 786.34 µs | 775.81 µs | — |
| 1 MiB | 13.00 ms | 12.97 ms | — |

<details><summary>Confidence intervals</summary>

| Size | Arm | Median | 95% interval |
|---:|---|---:|---|
| 16 B | `ferrosift` | 1.83 µs | 1.80 µs – 1.86 µs |
| 16 B | `ferrosift-per-call` | 2.91 µs | 2.89 µs – 2.95 µs |
| 256 B | `ferrosift` | 7.25 µs | 6.83 µs – 7.64 µs |
| 256 B | `ferrosift-per-call` | 7.93 µs | 6.63 µs – 8.32 µs |
| 4 KiB | `ferrosift` | 46.36 µs | 46.07 µs – 46.81 µs |
| 4 KiB | `ferrosift-per-call` | 57.19 µs | 49.13 µs – 58.13 µs |
| 64 KiB | `ferrosift` | 786.34 µs | 765.57 µs – 931.76 µs |
| 64 KiB | `ferrosift-per-call` | 775.81 µs | 772.49 µs – 780.87 µs |
| 1 MiB | `ferrosift` | 13.00 ms | 12.94 ms – 13.12 ms |
| 1 MiB | `ferrosift-per-call` | 12.97 ms | 12.90 ms – 13.04 ms |

</details>

### distance / levenshtein

| Size | `ferrosift` | `strsim-crate` | verdict |
|---:|---:|---:|---|
| 16 B | 2.61 µs | 479 ns | 5.45× slower |
| 64 B | 10.10 µs | 3.46 µs | 2.92× slower |
| 256 B | 68.71 µs | 50.41 µs | 1.36× slower |
| 1 KiB | 1.88 ms | 1.74 ms | 1.08× slower |

Verdict compares `ferrosift` against `strsim-crate`, the fastest
comparison arm in this group.

<details><summary>Confidence intervals</summary>

| Size | Arm | Median | 95% interval |
|---:|---|---:|---|
| 16 B | `ferrosift` | 2.61 µs | 2.47 µs – 2.71 µs |
| 16 B | `strsim-crate` | 479 ns | 462 ns – 518 ns |
| 64 B | `ferrosift` | 10.10 µs | 9.49 µs – 10.59 µs |
| 64 B | `strsim-crate` | 3.46 µs | 3.44 µs – 3.48 µs |
| 256 B | `ferrosift` | 68.71 µs | 68.25 µs – 69.41 µs |
| 256 B | `strsim-crate` | 50.41 µs | 50.07 µs – 50.72 µs |
| 1 KiB | `ferrosift` | 1.88 ms | 1.85 ms – 1.92 ms |
| 1 KiB | `strsim-crate` | 1.74 ms | 1.68 ms – 1.78 ms |

</details>

### hex / encode

| Size | `faster-hex-crate` | `ferrosift` | `ferrosift-per-call` | `hex-crate` | verdict |
|---:|---:|---:|---:|---:|---|
| 16 B | 62 ns | 733 ns | 1.54 µs | 104 ns | noisy — rerun |
| 256 B | 96 ns | 824 ns | 1.69 µs | 914 ns | 8.59× slower |
| 4 KiB | 873 ns | 4.44 µs | 5.31 µs | 14.99 µs | noisy — rerun |
| 64 KiB | 11.90 µs | 67.45 µs | 64.30 µs | 236.09 µs | 5.67× slower |
| 1 MiB | 492.21 µs | 1.57 ms | 1.58 ms | 3.84 ms | 3.19× slower |

Verdict compares `ferrosift` against `faster-hex-crate`, the fastest
comparison arm in this group.

<details><summary>Confidence intervals</summary>

| Size | Arm | Median | 95% interval |
|---:|---|---:|---|
| 16 B | `faster-hex-crate` | 62 ns | 57 ns – 65 ns |
| 16 B | `ferrosift` | 733 ns | 681 ns – 794 ns |
| 16 B | `ferrosift-per-call` | 1.54 µs | 1.46 µs – 1.63 µs |
| 16 B | `hex-crate` | 104 ns | 102 ns – 109 ns |
| 256 B | `faster-hex-crate` | 96 ns | 90 ns – 103 ns |
| 256 B | `ferrosift` | 824 ns | 790 ns – 872 ns |
| 256 B | `ferrosift-per-call` | 1.69 µs | 1.60 µs – 1.81 µs |
| 256 B | `hex-crate` | 914 ns | 861 ns – 974 ns |
| 4 KiB | `faster-hex-crate` | 873 ns | 794 ns – 930 ns |
| 4 KiB | `ferrosift` | 4.44 µs | 4.19 µs – 4.86 µs |
| 4 KiB | `ferrosift-per-call` | 5.31 µs | 5.12 µs – 5.74 µs |
| 4 KiB | `hex-crate` | 14.99 µs | 13.87 µs – 16.35 µs |
| 64 KiB | `faster-hex-crate` | 11.90 µs | 11.75 µs – 12.34 µs |
| 64 KiB | `ferrosift` | 67.45 µs | 64.88 µs – 71.95 µs |
| 64 KiB | `ferrosift-per-call` | 64.30 µs | 62.95 µs – 67.31 µs |
| 64 KiB | `hex-crate` | 236.09 µs | 228.07 µs – 257.17 µs |
| 1 MiB | `faster-hex-crate` | 492.21 µs | 473.39 µs – 505.59 µs |
| 1 MiB | `ferrosift` | 1.57 ms | 1.53 ms – 1.60 ms |
| 1 MiB | `ferrosift-per-call` | 1.58 ms | 1.54 ms – 1.69 ms |
| 1 MiB | `hex-crate` | 3.84 ms | 3.75 ms – 4.04 ms |

</details>

### overhead / identity

| Size | `compiled-pipeline` | `execute-each-call` | verdict |
|---:|---:|---:|---|
| 16 B | 589 ns | 983 ns | 1.67× faster |
| 256 B | 536 ns | 1.01 µs | noisy — rerun |
| 4 KiB | 626 ns | 1.09 µs | 1.74× faster |
| 64 KiB | 2.21 µs | 2.79 µs | 1.26× faster |
| 1 MiB | 295.46 µs | 289.68 µs | no measurable difference |

Verdict compares `compiled-pipeline` against `execute-each-call`, the fastest
comparison arm in this group.

<details><summary>Confidence intervals</summary>

| Size | Arm | Median | 95% interval |
|---:|---|---:|---|
| 16 B | `compiled-pipeline` | 589 ns | 544 ns – 614 ns |
| 16 B | `execute-each-call` | 983 ns | 949 ns – 1.06 µs |
| 256 B | `compiled-pipeline` | 536 ns | 507 ns – 593 ns |
| 256 B | `execute-each-call` | 1.01 µs | 956 ns – 1.14 µs |
| 4 KiB | `compiled-pipeline` | 626 ns | 572 ns – 662 ns |
| 4 KiB | `execute-each-call` | 1.09 µs | 1.03 µs – 1.16 µs |
| 64 KiB | `compiled-pipeline` | 2.21 µs | 2.04 µs – 2.32 µs |
| 64 KiB | `execute-each-call` | 2.79 µs | 2.71 µs – 2.90 µs |
| 1 MiB | `compiled-pipeline` | 295.46 µs | 280.60 µs – 310.38 µs |
| 1 MiB | `execute-each-call` | 289.68 µs | 280.44 µs – 295.59 µs |

</details>

### overhead / md5

| Size | `primitive-direct` | `through-recipe` | verdict |
|---:|---:|---:|---|
| 16 B | 145 ns | 1.25 µs | 8.64× slower |
| 256 B | 632 ns | 1.66 µs | 2.63× slower |
| 4 KiB | 7.69 µs | 8.81 µs | noisy — rerun |
| 64 KiB | 140.02 µs | 124.95 µs | no measurable difference |
| 1 MiB | 1.94 ms | 2.08 ms | no measurable difference |

Verdict compares `through-recipe` against `primitive-direct`, the fastest
comparison arm in this group.

<details><summary>Confidence intervals</summary>

| Size | Arm | Median | 95% interval |
|---:|---|---:|---|
| 16 B | `primitive-direct` | 145 ns | 137 ns – 153 ns |
| 16 B | `through-recipe` | 1.25 µs | 1.18 µs – 1.31 µs |
| 256 B | `primitive-direct` | 632 ns | 597 ns – 684 ns |
| 256 B | `through-recipe` | 1.66 µs | 1.61 µs – 1.74 µs |
| 4 KiB | `primitive-direct` | 7.69 µs | 7.09 µs – 8.26 µs |
| 4 KiB | `through-recipe` | 8.81 µs | 8.30 µs – 9.36 µs |
| 64 KiB | `primitive-direct` | 140.02 µs | 131.30 µs – 148.01 µs |
| 64 KiB | `through-recipe` | 124.95 µs | 119.14 µs – 131.34 µs |
| 1 MiB | `primitive-direct` | 1.94 ms | 1.77 ms – 2.02 ms |
| 1 MiB | `through-recipe` | 2.08 ms | 1.99 ms – 2.27 ms |

</details>

### overhead / sha256

| Size | `primitive-direct` | `through-recipe` | verdict |
|---:|---:|---:|---|
| 16 B | 68 ns | 1.50 µs | 22.12× slower |
| 256 B | 182 ns | 1.76 µs | 9.69× slower |
| 4 KiB | 1.81 µs | 3.69 µs | 2.04× slower |
| 64 KiB | 29.11 µs | 33.64 µs | noisy — rerun |
| 1 MiB | 462.37 µs | 745.79 µs | 1.61× slower |

Verdict compares `through-recipe` against `primitive-direct`, the fastest
comparison arm in this group.

<details><summary>Confidence intervals</summary>

| Size | Arm | Median | 95% interval |
|---:|---|---:|---|
| 16 B | `primitive-direct` | 68 ns | 65 ns – 71 ns |
| 16 B | `through-recipe` | 1.50 µs | 1.39 µs – 1.60 µs |
| 256 B | `primitive-direct` | 182 ns | 172 ns – 196 ns |
| 256 B | `through-recipe` | 1.76 µs | 1.64 µs – 1.85 µs |
| 4 KiB | `primitive-direct` | 1.81 µs | 1.67 µs – 1.92 µs |
| 4 KiB | `through-recipe` | 3.69 µs | 3.52 µs – 3.89 µs |
| 64 KiB | `primitive-direct` | 29.11 µs | 27.33 µs – 31.14 µs |
| 64 KiB | `through-recipe` | 33.64 µs | 29.67 µs – 35.79 µs |
| 1 MiB | `primitive-direct` | 462.37 µs | 435.79 µs – 492.93 µs |
| 1 MiB | `through-recipe` | 745.79 µs | 729.56 µs – 755.76 µs |

</details>

