[package]
name = "ferrosift-pattern"
description = "Pattern-language front end for FerroSift binary structure parsing"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]

[dev-dependencies]
# Test-only, and only to read the oracle's fixture. The crate itself carries no
# dependency at all, which is the point of the `pattern` feature pack.
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"

[lints]
workspace = true
